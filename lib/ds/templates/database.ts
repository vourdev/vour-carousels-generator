// Database mockup — two related tables (schema/ERD) with a relation glyph.
// v1.0 "Engineering Editorial" (SHOWCASE-mockups.html § "Database").
// Both tables + the relation glyph are pre-built (escaped) in the renderer.
export const databaseTemplate = String.raw`<div class="diag-wrap mt-40">
    <div class="db">DB_TABLES_INJECT</div>
  </div>`;
