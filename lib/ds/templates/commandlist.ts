// Command-list mockup — mono `cmd → desc` rows (CLI menus, shortcut lists).
// v1.0 "Engineering Editorial". Rows injected via sentinel (raw HTML).
export const commandListTemplate = String.raw`<div class="diag-wrap mt-40">
    <div class="clist">CLIST_ROWS_INJECT</div>
  </div>
  NOTE_INJECT`;
