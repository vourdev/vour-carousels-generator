**BrandMark + BrandHeader** — the @vourdev mark.

Always uses `assets/vourdev-logo.jpeg`. Never render as letters, SVG, or "VD" wordmark.

```jsx
<BrandMark size={72} />
<BrandHeader size={72} />
```

Pinned sizes: 72 (cover) · 44 (inner pill) · 40 (inside a card). Never smaller than 40.
