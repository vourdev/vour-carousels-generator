**NumberedStep** — orange-circled step card. Stack three vertically with 16 gap.

```jsx
<NumberedStep n={1} title="Start minimal"
  body="Your colors, fonts, and 3 hierarchy rules. Even that beats prompting from scratch." />
```

Update 2: badge is 40px (was 36), title 32px, body 28px — fixed values from the §7 table.
