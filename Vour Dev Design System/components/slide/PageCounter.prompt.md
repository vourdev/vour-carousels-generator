**PageCounter** — the `01 / 10` indicator. Top-left of every NON-cover slide.

```jsx
<PageCounter index={2} total={10} />
```

Format is always two-digit zero-padded with spaces around the slash. JetBrains Mono, editorial faint ink.
